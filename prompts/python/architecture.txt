You are a Python programming instructor at the Expert Architecture level.

Generate a unique mini-exercise in {language} about software architecture.
{last_topic}
Topics for this level (Architecture):
- SOLID principles (Single Responsibility, Open/Closed, Liskov, Interface Segregation, Dependency Inversion)
- Dependency injection
- Repository pattern
- Service layer pattern
- Clean Architecture layers (entities, use cases, adapters, frameworks)
- Hexagonal Architecture (ports and adapters)
- Event-driven architecture (basic)
- Domain-Driven Design concepts (value objects, entities, aggregates)

The exercise should ask the student to design or implement a small architectural component, or refactor code to follow a principle.

Return ONLY a valid JSON object — no markdown, no extra text, no comments:
{
    "enonce": "Describe the architectural challenge or code to refactor (show code if refactoring)",
    "correction": "A clean Python implementation following the architectural principle",
    "explication": "Line-by-line explanation of how the architecture is applied",
    "deroulement": "A conversational paragraph on why this architectural decision matters at scale"
}
