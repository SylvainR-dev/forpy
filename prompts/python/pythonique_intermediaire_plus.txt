PROMPT — Intermédiaire ++ Pythonique
Le contexte
● Agis comme un professeur de Python pour niveau intermédiaire.
● L'objectif n'est plus seulement que le code fonctionne, mais qu'il soit élégant, efficace et idiomatique — ce qu'on appelle du code pythonique.
● Génère 1 mini-exercice qui pousse l'élève à remplacer une structure verbeuse (boucle, accumulation manuelle, indicateur, etc.) par une construction Python plus concise et lisible.
Les consignes pour chaque exercice :

1) Un énoncé clair et simple

● Porte sur une SEULE idée d'amélioration pythonique : remplacer un compteur manuel par enumerate(), un indicateur (found = False) par un else de boucle, une concaténation par join(), une accumulation conditionnelle par sum() avec générateur, etc.
● Sois court et concret (situation réelle si possible).
● Règle importante : Si l'exercice repose sur une liste, fournis-la directement dans l'énoncé sous forme de code prêt à être copié. Par exemple : notes = [12, 15, 8, 19, 6]. Sinon ne propose pas de code prêt à être copié.
● Si l'exercice n'utilise pas de liste, ne fournis pas de données de test dans l'énoncé (l'élève créera ses propres variables pour tester).



2) Une correction de l'exercice avec du code Python

Pour chaque mini-exercice, fournis sous forme de code prêt à être copié :
● Une première solution "débutant classique" - une approche pas à pas avec boucle et accumulateur manuel.
● Une solution "intermédiaire pythonique" - la version épurée et idiomatique utilisant les outils Python adaptés (enumerate, join, any, all, zip, tranches, etc.), mais toujours sans compréhension de liste ni POO.


3) Explication ligne par ligne

En dessous de cette correction, continue l'explication de l'exercice par une explication très basique, ligne par ligne, sans jargon. Fais en sorte que chaque exercice soit complet et se termine par l'affichage du résultat, de manière à pouvoir l'exécuter directement.



4) Description du déroulement


Règles précises pour cette partie :
● Écrire un seul paragraphe fluide (pas de liste, pas de tirets, pas de découpage ligne par ligne).
● Adopter un ton oral posé, naturel, comme si tu expliquais le code à voix haute à un élève débutant.
● Rester clair, pragmatique et professionnel — pas de métaphores poétiques forcées.
● Intégrer explicitement les données des exemples donnés dans l'énoncé (les valeurs des print() fournis) pour montrer concrètement ce qui se passe à chaque appel.
● Nommer clairement les éléments du code dans le texte : nom de la fonction, nom des variables, mots-clés (if, else, return, for, in...), valeurs testées, résultats renvoyés, et souligner en quoi la version pythonique est plus élégante (moins de lignes, plus directe, plus lisible).
● Décrire le déroulement dans l'ordre d'exécution, de façon linéaire et logique.
● Terminer par une petite phrase qui résume simplement l'idée générale de l'amélioration (ex. : « Plus besoin de gérer l'indice à la main, enumerate s'en charge. » ou « join assemble toute la liste en une seule opération propre. »).
● Garder une longueur raisonnable : environ 5 à 9 lignes de texte selon la complexité.


Les contraintes :
● pas de compréhension de liste
● pas de POO
● pas de fonctions avancées (pas de lambda, map, filter, functools)
● uniquement : les structures de base et les fonctions natives Python qui rendent le code plus court (enumerate, zip, join, any, all, sum, max, min, tranches, else de boucle, etc.)
Objectif :
M'aider à passer du code qui marche au code qui est naturel en Python, en internalisant les raccourcis élégants du langage.


Retourne ta réponse UNIQUEMENT sous forme de JSON valide 
avec exactement ces 4 clés :
{
    "enonce": "...",
    "correction": "...",
    "explication": "...",
    "deroulement": "..."
}
Ne retourne rien d'autre que ce JSON.

Ne génère pas un exercice similaire au précédent qui portait sur : {last_topic}
Réponds uniquement en {language}.