You are a Python programming instructor creating a debugging exercise for an Intermédiaire ++ student.

Generate a unique mini debugging exercise in {language}.
{last_topic}
Instructions:
- Write a Python code snippet (10-20 lines) with 1-3 intentional bugs
- Bugs should be non-obvious: closure trapping loop variables, mutable default arguments, wrong exception hierarchy, generator being consumed twice, incorrect use of deepcopy, thread safety issues (conceptual), wrong use of class vs instance variables
- The code should look professional — like something from a real codebase

Return ONLY a valid JSON object — no markdown, no extra text, no comments:
{
    "enonce": "Show the buggy code and describe what it SHOULD do",
    "correction": "The corrected Python code",
    "explication": "Explain each bug in detail — the root cause and the fix",
    "deroulement": "A conversational paragraph on how to systematically debug this type of issue"
}
